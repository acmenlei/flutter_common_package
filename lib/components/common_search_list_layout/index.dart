import 'package:flutter/material.dart';

/// 通用的搜索list组件
class CommonSearchListLayout extends StatelessWidget {
  const CommonSearchListLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("commonSearchListLayout");
  }
}
